#!/usr/bin/chezscheme --script

(define prefix "/tmp/")
(load (car (command-line-arguments)))

(define source-file (cadr (command-line-arguments)))
(load source-file)
(graph-puts (format "~a~a" prefix source-file) #f)

(exit)
